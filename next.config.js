/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "uploads-ssl.webflow.com",
      "img.freepik.com",
    ],
  },
};

module.exports = nextConfig;
